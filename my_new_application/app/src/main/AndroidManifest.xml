<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions nécessaires pour l'Internet, la localisation et le stockage -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:theme="@style/Theme.My_new_application"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <!-- ✅ LoginActivity = écran de démarrage -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Driver → publier trajets -->
        <activity android:name=".MainActivity" />

        <!-- Passenger → liste trajets + réserver -->
        <activity android:name=".RidesListActivity" />

        <!-- Réservations passager -->
        <activity android:name=".BookingsActivity" />

        <!-- Avis -->
        <activity android:name=".ReviewActivity" />

        <!-- Carte -->
        <activity android:name=".MapActivity" />
        <activity android:name=".RegisterActivity" android:exported="false"/>

    </application>
</manifest>